#include <stdio.h>
//#define days 7
//#define citys 3
#define std 5
#define cor 3
int main() {

	//int i, j, tempc[citys][days];
	//float tempf[citys][days],tep=0;

	//for (i = 0; i < citys; i++) {
	//	if (i == 0)printf("Seoul\n");
	//	else if (i == 1)printf("New York\n");
	//	else printf("Beijing\n");
	//	for (j = 0; j < days; j++) {
	//		printf("DAY %d : ", j+1);
	//		scanf_s("%d", &tempc[i][j]);
	//		tempf[i][j] = (9.0 / 5.0) * tempc[i][j] + 32.0;
	//		tep += tempf[i][j];
	//	}
	//	printf("average tempF : %f\n", tep / days);
	//}


	//int grade[std][cor], i, j;
	//float tmp = 0, tmp_cor[std] = { 0.0 };
	//for (i = 0; i < std; i++) {
	//	tmp = 0;
	//	for (j = 0; j < cor; j++) {
	//		printf("TYPE %d student's grade for courses %d: ", i + 1,j+1);
	//		scanf_s("%d", &grade[i][j]);
	//		tmp += grade[i][j];
	//	}
	//	printf("%d student average : %f\n\n", i + 1, tmp / cor);
	//}
	//printf("\n");
	//for (j = 0; j < cor; j++) {
	//	tmp_cor[j] = 0;
	//	for (i = 0; i < std; i++) {
	//		tmp_cor[j] += grade[i][j];
	//	}
	//	printf("corses %d average : %f\n", j + 1, tmp_cor[j] / std);
	//}

	//char m[81] = "Program";
	//for (int i = 0; i < 9; i++) {
	//	printf("Address=%d content=%c(%3d)\n", &m[i], m[i], m[i]);
	//}

	//char arr;
	//arr = getchar();
	//putchar(arr);


}